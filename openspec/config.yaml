# OpenSpec — RAG (cursoIAExia-master)
# Auto-generated by sdd-init.

context: |
  .NET 10 RAG solution with PostgreSQL + pgvector for semantic search.
  Two entry points: a Minimal API (src/RAG.Api) and a legacy net9.0 MVC app (rag/).
  Clean Architecture: Domain → Application → Infrastructure → Api.
  AI layer via Ollama (IChatClient, IEmbeddingGenerator).

mode: hybrid

strict_tdd: false

testing:
  runner:
    command: ""
    framework: ""
  layers:
    unit: false
    integration: false
    e2e: false
  coverage:
    available: false
    command: ""
  quality:
    linter: false
    type_checker: true
    formatter: false

phases:
  propose: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
