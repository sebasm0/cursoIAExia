# OpenSpec — RAG (cursoIAExia-master)
# Auto-generated by sdd-init (re-init: 2026-07-30)

context: |
  .NET 10 RAG solution with PostgreSQL + pgvector (via Dapper + Npgsql)
  for semantic search. AI via Ollama (IChatClient, IEmbeddingGenerator).
  Two entry points: Minimal API (src/RAG.Api) and MVC ASP.NET Core app (rag/).
  Clean Architecture: Domain → Application → Infrastructure → Api/MVC.
  Tests: xUnit + Moq + WebApplicationFactory (tests/RAG.Mvc.Tests).

mode: hybrid

strict_tdd: true

testing:
  runner:
    command: "dotnet test"
    framework: "xUnit 2.9.3 / Microsoft.NET.Test.Sdk 17.13.0"
  layers:
    unit: true
    integration: true
    e2e: false
  coverage:
    available: false
    command: ""
  quality:
    linter: false
    type_checker: true
    formatter: false

phases:
  propose: {}
  spec: {}
  design: {}
  tasks: {}
  apply: {}
  verify: {}
  archive: {}
